---
description: .env / .env.local を削除・上書きしない
alwaysApply: true
---

# 環境変数ファイルの扱い

- **削除しない**: `.env`, `.env.local`, `.env.*` を削除するコマンド（`rm`, `git clean` で .env を対象にするなど）は実行しない。
- **上書きしない**: 上記ファイルの内容を書き換えない。
- ユーザーが明示的に依頼した場合を除き、これらのファイルに触れない。
